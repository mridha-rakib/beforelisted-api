services:
  - type: web
    name: beforelisted-api
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: ENABLE_SCHEDULED_JOBS
        value: true
